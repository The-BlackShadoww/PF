# Browser-facing API path. Requests to this path are proxied by Next.js/Vercel.
NEXT_PUBLIC_API_URL=/api/v1

# Server-only upstream API. Set this to the Render API URL in Vercel.
# API_PROXY_TARGET=https://your-render-service.onrender.com/api/v1
